# H1 : a >  b
t.test(x = data1$Popularity, y = data2$Popularity, alternative = "greater")
# me lo dijo liset
#pf(alfa, n1-1, n2-1) f de fisher
#qt(alfa, v) t de student
print(pf(0.975, 22, 26))
print(qt())
#print(qf(0.025, df1 = 22, df2 = 26)) # f de fisher
#print(qf(0.975, df1 = 22, df2 = 26)) # f de fisher
#print(qt(0.95, 37)) # t de student
data1 <- read.table(file.choose(), header = T)
data2 <- read.table(file.choose(), header = T)
t.test(x = data1$Popularity, y = data2$Popularity, alternative = "greater")
# Importamos la tabla con los géneros pop y sus derivados
data1 <- read.table(file.choose(), header = T)
# Importamos la tabla con los demás géneros musicales
data2 <- read.table(file.choose(), header = T)
# Prueba de hipótesis para la varianza de igualdad contra diferencia
var.test(data1$Popularity, data2$Popularity)
# Prueba de hipótesis para la media
t.test(x = data1$Popularity, y = data2$Popularity, alternative = "greater", var.equal = FALSE)
help(median)
MediaAritmetica <- function(x)
{
return (sum(x) / length(x))
}
Moda <- function(x)
{
set <- unique(x)
freq <- tabulate(match(x, set))
m <- max(freq)
return (set[freq == m])
}
Mediana <- function(x)
{
return (median(x))
}
DesviacionEstandar <- function(x)
{
return (sd(x))
}
Varianza <- function(x)
{
return (DesviacionEstandar(x) ^ 2)
}
CoeficienteVariacion <- function(x)
{
return (DesviacionEstandar(x) / MediaAritmetica(x))
}
InfoEstadisticosDescriptivos <- function(text, x)
{
print(text)
print(summary(x))
print(paste("Media Aritmética = ", MediaAritmetica(x)))
print(paste("Moda = ", Moda(x)))
print(paste("Mediana = ", Mediana(x)))
print(paste("Varianza = ", Varianza(x)))
print(paste("Desviación Estándar = ", DesviacionEstandar(x)))
print(paste("Coeficiente de Variación = ", CoeficienteVariacion(x)))
Quartiles(x)
}
Quartiles <- function(x)
{
quantile(x)
}
Histograma <- function(name, abcisa, x)
{
hist(x, main = name, xlab = abcisa)
}
CajaBigote <- function(name, x)
{
boxplot(x, main = name)
}
Graficos <- function(name, abcisa, x)
{
Histograma(name, abcisa, x)
CajaBigote(name, x)
}
# Importamos la tabla
data <- read.table(file.choose(), header = T)
# Imprimimos los estadísticos descriptivos de las variables escogidas
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Popularidad", data$Popularity)
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Acústica", data$Acousticness)
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Bailabilidad", data$Danceability)
# Ploteamos los gráficos de las variables aleatorias escogidas
Graficos("Observación Popularidad", "Values", data$Popularity)
Graficos("Observación Acústica", "Values", data$Acousticness)
Graficos("Observación Bailabilidad", "Values", data$Danceability)
MediaAritmetica <- function(x)
{
return (sum(x) / length(x))
}
Moda <- function(x)
{
set <- unique(x)
freq <- tabulate(match(x, set))
m <- max(freq)
return (set[freq == m])
}
Mediana <- function(x)
{
return (median(x))
}
DesviacionEstandar <- function(x)
{
return (sd(x))
}
Varianza <- function(x)
{
return (DesviacionEstandar(x) ^ 2)
}
CoeficienteVariacion <- function(x)
{
return (DesviacionEstandar(x) / MediaAritmetica(x))
}
InfoEstadisticosDescriptivos <- function(text, x)
{
print(text)
print(summary(x))
print(paste("Media Aritmética = ", MediaAritmetica(x)))
print(paste("Moda = ", Moda(x)))
print(paste("Mediana = ", Mediana(x)))
print(paste("Varianza = ", Varianza(x)))
print(paste("Desviación Estándar = ", DesviacionEstandar(x)))
print(paste("Coeficiente de Variación = ", CoeficienteVariacion(x)))
Quartiles(x)
}
Quartiles <- function(x)
{
quantile(x)
}
Histograma <- function(name, abcisa, x)
{
hist(x, main = name, xlab = abcisa)
}
CajaBigote <- function(name, x)
{
boxplot(x, main = name)
}
Graficos <- function(name, abcisa, x)
{
Histograma(name, abcisa, x)
CajaBigote(name, x)
}
# Importamos la tabla
data <- read.table(file.choose(), header = T)
# Imprimimos los estadísticos descriptivos de las variables escogidas
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Popularidad", data$Popularity)
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Acústica", data$Acousticness)
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Bailabilidad", data$Danceability)
# Ploteamos los gráficos de las variables aleatorias escogidas
Graficos("Observación Popularidad", "Values", data$Popularity)
Graficos("Observación Acústica", "Values", data$Acousticness)
Graficos("Observación Bailabilidad", "Values", data$Danceability)
ariel <- "asdjlasd"
print(ariel)
MediaAritmetica <- function(x)
{
return (sum(x) / length(x))
}
Moda <- function(x)
{
set <- unique(x)
freq <- tabulate(match(x, set))
m <- max(freq)
return (set[freq == m])
}
Mediana <- function(x)
{
return (median(x))
}
DesviacionEstandar <- function(x)
{
return (sd(x))
}
Varianza <- function(x)
{
return (DesviacionEstandar(x) ^ 2)
}
CoeficienteVariacion <- function(x)
{
return (DesviacionEstandar(x) / MediaAritmetica(x))
}
InfoEstadisticosDescriptivos <- function(text, x)
{
print(text)
print(summary(x))
print(paste("Media Aritmética = ", MediaAritmetica(x)))
print(paste("Moda = ", Moda(x)))
print(paste("Mediana = ", Mediana(x)))
print(paste("Varianza = ", Varianza(x)))
print(paste("Desviación Estándar = ", DesviacionEstandar(x)))
print(paste("Coeficiente de Variación = ", CoeficienteVariacion(x)))
Quartiles(x)
}
Quartiles <- function(x)
{
quantile(x)
}
Histograma <- function(name, abcisa, x)
{
hist(x, ylab = "qwe", xlab = abcisa, main = name)
}
CajaBigote <- function(name, x)
{
boxplot(x, main = name)
}
Graficos <- function(name, abcisa, x)
{
Histograma(name, abcisa, x)
CajaBigote(name, x)
}
# Importamos la tabla
data <- read.table(file.choose(), header = TRUE)
d <- read.csv(header = T)
# Imprimimos los estadísticos descriptivos de las variables escogidas
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Popularidad", data$Popularity)
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Acústica", data$Acousticness)
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Bailabilidad", data$Danceability)
# Ploteamos los gráficos de las variables aleatorias escogidas
Graficos("Observación Popularidad", "Values", data$Popularity)
Graficos("Observación Acústica", "Values", data$Acousticness)
Graficos("Observación Bailabilidad", "Values", data$Danceability)
ariel <- "asdjlasd"
print(ariel)
MediaAritmetica <- function(x)
{
return (sum(x) / length(x))
}
Moda <- function(x)
{
set <- unique(x)
freq <- tabulate(match(x, set))
m <- max(freq)
return (set[freq == m])
}
Mediana <- function(x)
{
return (median(x))
}
DesviacionEstandar <- function(x)
{
return (sd(x))
}
Varianza <- function(x)
{
return (DesviacionEstandar(x) ^ 2)
}
CoeficienteVariacion <- function(x)
{
return (DesviacionEstandar(x) / MediaAritmetica(x))
}
InfoEstadisticosDescriptivos <- function(text, x)
{
print(text)
print(summary(x))
print(paste("Media Aritmética = ", MediaAritmetica(x)))
print(paste("Moda = ", Moda(x)))
print(paste("Mediana = ", Mediana(x)))
print(paste("Varianza = ", Varianza(x)))
print(paste("Desviación Estándar = ", DesviacionEstandar(x)))
print(paste("Coeficiente de Variación = ", CoeficienteVariacion(x)))
Quartiles(x)
}
Quartiles <- function(x)
{
quantile(x)
}
Histograma <- function(name, abcisa, x)
{
hist(x, ylab = "qwe", xlab = abcisa, main = name)
}
CajaBigote <- function(name, x)
{
boxplot(x, main = name)
}
Graficos <- function(name, abcisa, x)
{
Histograma(name, abcisa, x)
CajaBigote(name, x)
}
# Importamos la tabla
data <- read.table(file.choose(), header = TRUE)
d <- read.csv(header = T)
# Imprimimos los estadísticos descriptivos de las variables escogidas
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Popularidad", data$Popularity)
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Acústica", data$Acousticness)
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Bailabilidad", data$Danceability)
# Ploteamos los gráficos de las variables aleatorias escogidas
Graficos("Observación Popularidad", "Values", data$Popularity)
Graficos("Observación Acústica", "Values", data$Acousticness)
Graficos("Observación Bailabilidad", "Values", data$Danceability)
ariel <- "asdjlasd"
print(ariel)
help(for)
help(while)
help("while")
MediaAritmetica <- function(x)
{
return (sum(x) / length(x))
}
Moda <- function(x)
{
set <- unique(x)
freq <- tabulate(match(x, set))
m <- max(freq)
return (set[freq == m])
}
Mediana <- function(x)
{
return (median(x))
}
DesviacionEstandar <- function(x)
{
return (sd(x))
}
Varianza <- function(x)
{
return (DesviacionEstandar(x) ^ 2)
}
CoeficienteVariacion <- function(x)
{
return (DesviacionEstandar(x) / MediaAritmetica(x))
}
InfoEstadisticosDescriptivos <- function(text, x)
{
print(text)
print(summary(x))
print(paste("Media Aritmética = ", MediaAritmetica(x)))
print(paste("Moda = ", Moda(x)))
print(paste("Mediana = ", Mediana(x)))
print(paste("Varianza = ", Varianza(x)))
print(paste("Desviación Estándar = ", DesviacionEstandar(x)))
print(paste("Coeficiente de Variación = ", CoeficienteVariacion(x)))
Quartiles(x)
}
Quartiles <- function(x)
{
quantile(x)
}
Histograma <- function(name, abcisa, x)
{
hist(x, ylab = "qwe", xlab = abcisa, main = name)
}
CajaBigote <- function(name, x)
{
boxplot(x, main = name)
}
Graficos <- function(name, abcisa, x)
{
Histograma(name, abcisa, x)
CajaBigote(name, x)
}
# Importamos la tabla
data <- read.table(file.choose(), header = TRUE)
d <- read.csv(header = T)
# Imprimimos los estadísticos descriptivos de las variables escogidas
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Popularidad", data$Popularity)
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Acústica", data$Acousticness)
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Bailabilidad", data$Danceability)
# Ploteamos los gráficos de las variables aleatorias escogidas
Graficos("Observación Popularidad", "Values", data$Popularity)
Graficos("Observación Acústica", "Values", data$Acousticness)
Graficos("Observación Bailabilidad", "Values", data$Danceability)
for(i in data$Track_Name)
{
print(i)
}
MediaAritmetica <- function(x)
{
return (sum(x) / length(x))
}
Moda <- function(x)
{
set <- unique(x)
freq <- tabulate(match(x, set))
m <- max(freq)
return (set[freq == m])
}
Mediana <- function(x)
{
return (median(x))
}
DesviacionEstandar <- function(x)
{
return (sd(x))
}
Varianza <- function(x)
{
return (DesviacionEstandar(x) ^ 2)
}
CoeficienteVariacion <- function(x)
{
return (DesviacionEstandar(x) / MediaAritmetica(x))
}
InfoEstadisticosDescriptivos <- function(text, x)
{
print(text)
print(summary(x))
print(paste("Media Aritmética = ", MediaAritmetica(x)))
print(paste("Moda = ", Moda(x)))
print(paste("Mediana = ", Mediana(x)))
print(paste("Varianza = ", Varianza(x)))
print(paste("Desviación Estándar = ", DesviacionEstandar(x)))
print(paste("Coeficiente de Variación = ", CoeficienteVariacion(x)))
Quartiles(x)
}
Quartiles <- function(x)
{
quantile(x)
}
Histograma <- function(name, abcisa, x)
{
hist(x, ylab = "qwe", xlab = abcisa, main = name)
}
CajaBigote <- function(name, x)
{
boxplot(x, main = name)
}
Graficos <- function(name, abcisa, x)
{
Histograma(name, abcisa, x)
CajaBigote(name, x)
}
# Importamos la tabla
data <- read.table(file.choose(), header = TRUE)
d <- read.csv(header = T)
# Imprimimos los estadísticos descriptivos de las variables escogidas
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Popularidad", data$Popularity)
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Acústica", data$Acousticness)
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Bailabilidad", data$Danceability)
# Ploteamos los gráficos de las variables aleatorias escogidas
Graficos("Observación Popularidad", "Values", data$Popularity)
Graficos("Observación Acústica", "Values", data$Acousticness)
Graficos("Observación Bailabilidad", "Values", data$Danceability)
print(data$Track_Name[0])
for(i in data$Track_Name)
{
print(i)
}
MediaAritmetica <- function(x)
{
return (sum(x) / length(x))
}
Moda <- function(x)
{
set <- unique(x)
freq <- tabulate(match(x, set))
m <- max(freq)
return (set[freq == m])
}
Mediana <- function(x)
{
return (median(x))
}
DesviacionEstandar <- function(x)
{
return (sd(x))
}
Varianza <- function(x)
{
return (DesviacionEstandar(x) ^ 2)
}
CoeficienteVariacion <- function(x)
{
return (DesviacionEstandar(x) / MediaAritmetica(x))
}
InfoEstadisticosDescriptivos <- function(text, x)
{
print(text)
print(summary(x))
print(paste("Media Aritmética = ", MediaAritmetica(x)))
print(paste("Moda = ", Moda(x)))
print(paste("Mediana = ", Mediana(x)))
print(paste("Varianza = ", Varianza(x)))
print(paste("Desviación Estándar = ", DesviacionEstandar(x)))
print(paste("Coeficiente de Variación = ", CoeficienteVariacion(x)))
Quartiles(x)
}
Quartiles <- function(x)
{
quantile(x)
}
Histograma <- function(name, abcisa, x)
{
hist(x, ylab = "qwe", xlab = abcisa, main = name)
}
CajaBigote <- function(name, x)
{
boxplot(x, main = name)
}
Graficos <- function(name, abcisa, x)
{
Histograma(name, abcisa, x)
CajaBigote(name, x)
}
# Importamos la tabla
data <- read.table(file.choose(), header = TRUE)
d <- read.csv(header = T)
# Imprimimos los estadísticos descriptivos de las variables escogidas
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Popularidad", data$Popularity)
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Acústica", data$Acousticness)
InfoEstadisticosDescriptivos("Estadísticos descriptivos de la variable aleatoria Bailabilidad", data$Danceability)
# Ploteamos los gráficos de las variables aleatorias escogidas
Graficos("Observación Popularidad", "Values", data$Popularity)
Graficos("Observación Acústica", "Values", data$Acousticness)
Graficos("Observación Bailabilidad", "Values", data$Danceability)
print(data$Track_Name[1])
for(i in data$Track_Name)
{
print(i)
}
